spring:
  application:
    name: '@project.artifactId@'
  datasource:
    url: jdbc:h2:mem:care-backend
    username: sa
    password: 1234

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true